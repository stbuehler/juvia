<%= render :template => 'api/base.js' %>
<%
  options = {
    :action    => 'ShowError',
    :container => @container,
    :site_key  => @site_key,
    :topic_key => @topic_key,
    :html      => render(:partial => 'internal_error.html')
  }
%>
Juvia.onApiLoad(function() {
	<%= juvia_handle_response(options) %>
});
