<%= render :template => 'api/base.js' %>
<%
  options = {
    :action    => 'ShowError',
    :container => @container,
    :site_key  => @site_key,
    :topic_key => @topic_key,
    :html      => render(:partial => 'site_not_found.html', :locals => { :topic => @topic })
  }
%>
Juvia.onApiLoad(function() {
	<%= juvia_handle_response(options) %>
});
