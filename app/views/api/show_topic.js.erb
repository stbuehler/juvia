<%= render :template => 'api/base.js' %>
<%
  options = {
    :action    => 'LoadTopic',
    :container => @container,
    :html      => render(:partial => 'topic.html', :locals => { :topic => @topic })
  }
%>
Juvia.onApiLoad(function() {
	<%= juvia_handle_response(options) %>
});
